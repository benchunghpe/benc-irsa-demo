apiVersion: v1
kind: Service
metadata:
  name: {{ include "irsa-demo.fullname" . }}
  labels:
    {{- include "irsa-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "irsa-demo.selectorLabels" . | nindent 4 }}
